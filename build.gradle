apply plugin: 'com.android.application'
android {
    compileSdkVersion 30
    defaultConfig {
        applicationId "com.example.alivip"
        minSdkVersion 21
        targetSdkVersion 30
    }
}
dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}
